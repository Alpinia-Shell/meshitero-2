<p>ユーザープロフィール画像</p>
<%= image_tag @user.get_profile_image(100,100) %>
<p>ユーザー名<p>
<% if @user == current_user %>
  <%= link_to 'プロフィール編集', edit_user_path(@user.id) %>
<% end %>

<%= render 'post_images/list', post_images: @post_image %>